# 环境配置（可移植）：NODE/PROTOCHAIN 留空则用 PATH 中的 node/protochain
# 路径约定：MODELING_DIR/IMPL_DIR 可为绝对或相对路径；相对路径以「实例目录」（本文件 env/ 的上级，即 projectDir）为基准
# 首轮（建模自举）只需 MODELING_DIR；接入真实 verify（参考实现/部署目标）时再按需添加：
#   PORT、VERIFY_TARGET、ADMIN_TOKEN/PORTAL_TOKEN/INSTANCE_TOKEN
NODE=
PROTOCHAIN=
MODELING_DIR={{MODELING_DIR}}
IMPL_DIR={{IMPL_DIR}}
