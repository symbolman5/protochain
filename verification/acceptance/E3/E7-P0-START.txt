================================================================================
E7 P0 开始提示词（E7-P0-START）
================================================================================

> 版本：v0.5（2026-08-22）
> 起点：T1 完成（E1 ✓ + E2 ✓ + E3 ✓）后启动
> 范围：Web 检阅界面 P0（只读静态生成，附录 B.4）
> 产物预期：[verification/acceptance/E7-P0/](file:///work/protochain/verification/acceptance/E7-P0/)

────────────────────────────────────────────────────────────────────────────────
1. T1 闭环状态
────────────────────────────────────────────────────────────────────────────────

| E# | 增强 | 状态 | 产物路径 |
|---|---|---|---|
| E1 | M 单元语义闸门（m-check） | ✓ PASS | [verification/acceptance/E1/](file:///work/protochain/verification/acceptance/E1/) |
| E2 | specs.json 升级 JSON Schema | ✓ PASS | [verification/acceptance/E2/](file:///work/protochain/verification/acceptance/E2/) |
| E3 | binding 骨架自动生成 | ✓ PASS | [verification/acceptance/E3/](file:///work/protochain/verification/acceptance/E3/) |
| **E7 P0** | **Web 检阅界面（只读静态）** | **→ 本任务** | verification/acceptance/E7-P0/ |

构建基线
  - npx tsc --noEmit → 0 errors
  - npx jest → 57 suites / 796 cases 全过
  - protochain CLI：derive-specs / derive-bindings / bind --skeleton / verify 均端到端可用

────────────────────────────────────────────────────────────────────────────────
2. 本任务实现规划（IMPLEMENTATION-PLAN.md §E7）
────────────────────────────────────────────────────────────────────────────────

严格按以下范围实施 P0（只读检阅，纯机械）：

2.1 新增命令 `derive-web`
   - 输入：derived/*.json（specs/contracts/test-cases/verification）+ model-diff.json + impact-analysis.json
   - 输出：web/data.json（静态站点数据）+ 静态站点产物（VitePress）
   - 模块落点：src/webgen/index.ts（与 src/bindgen/ 命名一致；与 web/ 站点工程分离）
   - 站点工程：web/（VitePress 静态站点）

2.2 静态站点页面（4 类）
   - 接口列表 / 详情（参数 + requestSchema/responseSchema + 状态机图）
   - 测试用例浏览器（路径 / 覆盖度 / 场景映射）
   - 验证报告对比（legacy vs impl 并排；V3 双跑对账）
   - 模型 diff / impact 可视化（改动 → 受影响接口 / 用例 / 骨架）

2.3 新增命令 `protochain web serve`
   - 一条命令起静态服务（VitePress 内置 vite preview）

2.4 P0 强依赖
   - E2（specs.json 有 schema 才能在 Web 展示接口详情）—— ✓ 已就绪
   - 与 E3 正交（E3 让 web 站点 P1 编辑场景有内容；P0 仅只读不需要骨架）

2.5 不在 P0 范围（明确防范围蔓延，附录 B.4）
   - mock 服务 / 团队协作 / 环境管理 / Apifox 克隆
   - 模型侧输入管理（在线编辑 scenarios / bindings）
   - Web 服务接触令牌环境变量

────────────────────────────────────────────────────────────────────────────────
3. 依赖（与 E3 同样的不引入新依赖原则）
────────────────────────────────────────────────────────────────────────────────

复用：
  - ajv@^8.17.1（已用 E2 / ajv 编译 specs）
  - yaml@^2.6.0（已用 E3 / YAML 序列化）
  - handlebars@^4.7.8（已用 scaffolder / 模板引擎）

新增（仅站点工程，不入工具链主依赖）：
  - vitepress@^1.x（devDependency；P0 范围纯静态，不引入运行时依赖）
  - mermaid（devDependency；状态机图渲染）

不引入：
  - 后端服务（Fastify/Express 不在 P0 范围）
  - 鉴权 / 团队协作相关依赖
  - 任何运行时 Web 框架

────────────────────────────────────────────────────────────────────────────────
4. 验收标准（IMPLEMENTATION-ACCEPTANCE.md §E7 P0）
────────────────────────────────────────────────────────────────────────────────

| 验收 | 通过标准 |
|---|---|
| derive-web 100% 机械 | 关闭 AI 也能跑（不依赖 generate-cases 等 AI 步骤产物） |
| web/data.json + 静态站点产物落盘 | web/data.json + web/.vitepress/dist/ 双产物 |
| protochain web serve 起服务 | 浏览器打开 4 类页面均可访问 |
| 四类页面人读（非裸 JSON） | 接口详情有结构展示 / 测试用例可点开 / 双跑报告并列 / diff 可视化 |
| P0 无运行时依赖 | 停掉 web serve 后静态文件仍可独立托管展示 |
| 安全 | web 产物不接触 authConfig.token / stateMap.secret 等敏感字段 |

────────────────────────────────────────────────────────────────────────────────
5. 产出要求（按 E1/E2/E3 同样的流程）
────────────────────────────────────────────────────────────────────────────────

5.1 设计 → design-notes.md 归档到 verification/acceptance/E7-P0/
   - 顶层 schema 设计（web/data.json 形态）
   - 四类页面布局 / 数据需求 / 渲染策略（VitePress + mermaid）
   - 与 E2/E3 的耦合点（specs.json schema / bindings.skeleton.yaml）
   - 静态站点的安全边界（敏感字段过滤）

5.2 实现
   - src/webgen/index.ts（derive-web 主体）
   - web/ 站点工程（VitePress config + 4 类页面组件）
   - src/cli/index.ts 注册 derive-web + web serve 命令

5.3 jest 测试覆盖
   - 正常协议：derive-web 产物四类页面数据完整
   - 空数据 / 边界（大协议 40+ 接口）
   - 敏感字段过滤（authConfig.token 不入 web 产物）
   - web serve 启动后 4 类 URL 可访问（HTTP 探针）

5.4 build（npx tsc 通过）+ npx jest 全过

5.5 验收证据归档到 verification/acceptance/E7-P0/
   - acceptance-record.yaml（结构同 E1/E2/E3）
   - web/data.json snapshot + 4 类页面 HTML snapshot
   - derive-web CLI 输出 + web serve 启动日志

5.6 完成后输出
   - E7 P0 验收提示词（结构同 E3-COMPLETE）
   - T1 全景总结提示词（E1 ✓ + E2 ✓ + E3 ✓ + E7 P0 ✓）

────────────────────────────────────────────────────────────────────────────────
6. 与现有 E# 的接口
────────────────────────────────────────────────────────────────────────────────

| 接口 | 来源 | P0 用途 |
|---|---|---|
| specs.json（Envelope） | E2 | 接口列表/详情页 |
| test-cases.json | E2 | 测试用例浏览器 |
| verification-report.json | E2（含字段级偏差） | 双跑报告对比 |
| bindings.skeleton.yaml | E3（可选；P0 只读不需要） | P1 准备；P0 留接口 |
| model-diff.json / impact-analysis.json | versioner | diff / impact 可视化 |
| implcheck 报告 | implcheck | 实现完整性状态 |

────────────────────────────────────────────────────────────────────────────────
7. 启动准备
────────────────────────────────────────────────────────────────────────────────

代码基线
  - 当前在 /work/protochain/（protochain 工具链根）
  - src/bindgen/index.ts 已实现（E3 产物，可复用）
  - src/specifier/{envelope,load,index}.ts 已实现（E2 产物，强依赖）
  - tests/bindgen/bindgen.test.ts 48 单测已就位

建议启动步骤
  1. 读 IMPLEMENTATION-PLAN.md §E7 + IMPLEMENTATION-ACCEPTANCE.md §E7 P0
  2. 读 verification/acceptance/E2/design-notes.md + E3/design-notes.md 熟悉上游产物
  3. 在 verification/acceptance/E7-P0/ 写 design-notes.md（先设计后实现）
  4. 实施 src/webgen/index.ts（derive-web 主体）+ web/ 站点工程（VitePress）
  5. 写 tests/webgen/ 单测 + 端到端 CLI 验证
  6. 跑 npx tsc --noEmit + npx jest
  7. 归档证据到 verification/acceptance/E7-P0/
  8. 输出 E7-P0-COMPLETE + T1 全景总结

────────────────────────────────────────────────────────────────────────────────
8. 不要做
────────────────────────────────────────────────────────────────────────────────

- 不要在 P0 引入任何运行时依赖（仅 devDependency vitepress）
- 不要碰 P1 范围（在线编辑 / 一键执行 / 评审标注生成修改单草稿）
- 不要修改 IMPLEMENTATION-PLAN.md / IMPLEMENTATION-ACCEPTANCE.md（除非发现规划缺陷；规划修订单独登记）
- 不要直接改 derived/*.json（保持权威源单一性）
- 不要碰 E4/E5/E6/E8/E9/E10（不在 T1 范围）

────────────────────────────────────────────────────────────────────────────────
9. 关联修改单登记位
────────────────────────────────────────────────────────────────────────────────

后续如发现 E7-P0 实施问题，登记到 IMPLEMENTATION-ISSUES.md §3「后续 E# 预留」之后新建 §4「E7 阶段问题」，按 E1-I1 / E2-I1 模式登记（ID 前缀 E7-I1）。
================================================================================
