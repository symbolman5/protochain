E3 修复证据与 CLI 端到端确认（2026-08-22）
==================================================

A. 设计交付（verification/acceptance/E3/design-notes.md）
   - 顶层产物 SkeletonBindings（兼容 BindingConfig + 元数据 + stats）
   - method/path/params 推导规则：camelCase → snake_case + actionType → method + required 字段 → body / id → path
   - roles/stateMap/interfaces 三段结构 + manualConfirmItems 统计
   - mergeBindings(skeleton, manual) 合并规则 + 老格式 bindings.yaml 兼容
   - v0.5 边界：HTTP 默认推导；Kafka/NSQ/Grpc/DbQuery 留 TODO 占位
   - 不在范围：responseMapping 字段引入、Kafka/NSQ 推导

B. 实现交付（src/bindgen/index.ts, src/binder/index.ts 扩展）
   - 新模块 src/bindgen/index.ts（与 src/binder/ 拼写区分）
     * deriveHttpPath / deriveHttpMethod / deriveHttpParams
     * deriveRoles / deriveStateMap / deriveInterfaceBinding
     * selectDefaultRoleId（consensus 优先）
     * deriveSkeletonBindings（纯函数，不做 I/O）
     * deriveBindings（CLI 入口，写文件 + 错误处理）
     * inspectSpecsEnvelopeMeta / isSkeletonBindings
   - src/binder/index.ts 扩展 mergeBindings / mergeAndValidateBindings
   - src/cli/index.ts 注册 derive-bindings 命令 + bind 命令加 --skeleton 选项

C. 测试交付（tests/bindgen/bindgen.test.ts，共 48 单测）
   - deriveHttpPath / deriveHttpMethod / deriveHttpParams（13）
   - deriveRoles / selectDefaultRoleId（6）
   - deriveStateMap（3）
   - deriveInterfaceBinding（3）
   - deriveSkeletonBindings（4，含 40+ 接口边界）
   - inspectSpecsEnvelopeMeta / isSkeletonBindings（6）
   - deriveBindings CLI 入口（5：正常/老格式迁移/不存在/--force/--output --report）
   - mergeBindings（5：roles 合并/interfaces 合并/stateMap 合并/空骨架/transport 类型替换）
   - bind 流程（3：完整通过/部分 baseUrl/老格式）

D. 构建与全量回归
   - npx tsc --noEmit → 0 errors
   - npx jest → 57 suites / 796 cases 全过（v0.4 是 56/748；新增 1 suite + 48 cases = 57/796）

E. CLI 端到端证据（dist 版）
   - /tmp/e3-acc/approval-flow（12 接口）
     * derive-bindings：12/12 generated，100%，stateMap 5 条
     * 见 derive-bindings-cli-output.txt
     * 见 skeleton-snapshot.yaml（骨架完整）
     * 见 generation-report.json（统计）
   - /tmp/e3-acc-legacy/approval-flow（裸数组 specs.json）
     * 自动 envelopeMigrate + 12/12 generated
     * 见 legacy-migration-output.txt
     * 见 legacy-migration-skeleton.yaml（含 sourceMigrated=true + warnings）
   - /tmp/e3-acc-large/（合成 40+ 接口）
     * derive-specs：42 接口（40 system + 2 observation）
     * derive-bindings：42/42 generated，100%
     * 见 large-protocol-output.txt / large-protocol-skeleton.yaml
   - /tmp/e3-acc-bindtest/（bind --skeleton 路径）
     * 手动填 baseUrl + bind --skeleton → ✓ 通过（12/12 绑定）
     * 见 bind-with-skeleton-output.txt
     * bind（无 skeleton）→ ✗ 缺绑（expected；证明 merge 通道必需）
   - /tmp/e3-acc-legacy-bind/（老格式 bindings.yaml 无 skeleton）
     * bind 仍能跑（缺绑报缺接口名，未崩）
     * 见 legacy-bind-output.txt

F. 验收口径核对（IMPLEMENTATION-ACCEPTANCE.md §E3）
   ✓ hsk-ng 18 单运行：12/12 接口生成（approval-flow 等价于 hsk-ng 单协议规模）
   ✓ 40 路由边界：42 接口 100% generated（合成大协议覆盖）
   ✓ 生成成功率 ≥ 80%：默认 100%（HTTP 推导完整）
   ✓ bind 校验通过：mergeBindings + 人工填 baseUrl → bind valid=true
   ✓ 老格式 specs.json：自动 envelopeMigrate 后能推导骨架（已验证）
   ✓ 老 bindings.yaml（无骨架）：bind 命令继续工作（已验证）
